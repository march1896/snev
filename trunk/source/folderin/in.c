#include "in.h"
#include "stdio.h"
int in_func()
{
	if ( 1 + 2 == 3 )
		return 0;
	if ( 2 + 2 == 4 )
		return 2;
	printf("out put something\n" );

	// hello, this is added in version 7

	else return 1;
}
